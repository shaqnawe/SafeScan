# Anthropic API key — get yours at https://console.anthropic.com/
ANTHROPIC_API_KEY=your-anthropic-api-key-here

# Database connection string
DATABASE_URL=postgresql://localhost:5432/safescan

# UPCitemdb barcode fallback — no API key required.
# Uses the trial endpoint (100 lookups/day per IP, rate limited by UPCitemdb).
# https://www.upcitemdb.com/api/explorer#!/lookup/get_trial_lookup
