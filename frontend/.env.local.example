# Copy this file to .env.local and fill in your machine's local IP address.
# Find your IP with: ipconfig getifaddr en0   (macOS Wi-Fi)
#
# This lets your phone reach the backend when on the same Wi-Fi network.

VITE_API_URL=http://192.168.x.x:8000
